---
alwaysApply: true
---

# 项目规则

> 首次使用时执行 /init 让 AI 扫描代码并填充本文件，反映项目**实际**的编码风格和规范。

## 1. 应用概况

- 应用名: （待填充）
- 简介: （一句话描述）
- 技术栈: （待填充，如 Java 21 / Spring Boot 3.x / MySQL）
- 构建工具: （待填充，如 Maven / Gradle / npm / pnpm）

## 2. 目录结构与分层架构

> 执行 `tree -d -L 3` 后填充。

```
Controller (web/)       <- 入口层，参数校验 + 协议转换
   |
Service (service/)      <- 业务编排，事务边界
   |
DAO (dao/)              <- 纯数据访问
```

## 3. 命名规范

- 类名：（待填充，如"大驼峰，见名知意"）
- 方法名：（待填充，如"小驼峰，动词开头"）
- 常量：（待填充，如"全大写下划线分隔"）
- 测试类：（待填充，如"被测类名 + Test 结尾"）

## 4. 异常处理

- （待填充，如"业务异常使用自定义 BizException，携带错误码"）
- （待填充，如"禁止吞掉异常（空 catch），catch 中必须记录日志"）

## 5. 日志规范

- （待填充，如"Controller 入口打 INFO，异常打 ERROR 含完整堆栈"）
- 禁止在日志中打印用户敏感信息

## 6. 关键依赖

| 中间件/库 | 用途 | 备注 |
|-----------|------|------|
| （待填充） | | |

## 7. 其他约定

- （待填充，如"写接口必须考虑幂等"）
- （待填充，如"魔法值必须定义为常量"）
